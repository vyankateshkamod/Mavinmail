import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Suppress the workspace root warning by being explicit about our setup
  // Note: If you still get Turbopack errors, you may need to upgrade Next.js
  // or configure the workspace root differently
};

export default nextConfig;
